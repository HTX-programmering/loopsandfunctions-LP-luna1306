String smth = "Hej med dig!";
 int index=-2;
 
 while (index<=5){ // her bruger du en konstant istedet for en variable. Hvis du bruger smth.length
 index=index+1;
 println(smth.charAt(index+1));
 }
 
 //Opgave 1.2
 
 String smth = "Hej med dig!";
 int index;
 
 for (index=-1; index<=5;index=index+1){ // din løkke vil have 7 iterationer!. Hold dig til 0 og længden på strengen og brug dit index ugen at tilføje en værdi.
 println(smth.charAt(index+1));
 }
