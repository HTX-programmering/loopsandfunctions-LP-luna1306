 
 void setup(){
   size(800, 800);
 }
 
 void draw(){
   circel(200);
   firkant(100, 100);
 }
 
 void circel(int bredde){
      fill(255,0,0);
   ellipse(width/2, height/2, bredde, bredde);

 }
 
 void firkant(float længde, float højde){
      fill(0, 0, 255);
   rect(0,0, længde, højde);

 }
